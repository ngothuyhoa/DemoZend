<h3> Dang Ky</h3>
<div class="container">
  <div class="row">
    <div class="col-md-6 col-mf-offset-3">
    	<?php
    		$form = $this->form;

    		$form->prepare();
    		$form->setAttribute('action', $this->url('edit', [], true));
    		$form->setAttribute('method', 'post');
    		$form->setAttribute('class', 'form-horizontal');
    		$formLabel = $this->plugin('formLabel');
    		echo $this->form()->openTag($form);
    	?>
		<div class="form-group">
			<?php
				$name = $form->get('user_name');
				echo $formLabel->openTag() . $name->getOption('label');
				$name->setValue($row['user_name']);
				//echo $formlabel->closeTag();
				echo $this->formInput($name);
				echo $this->formElementErrors($name);
			?>
	  	</div>
	  	<div class="form-group">
		    <?php
				$email = $form->get('email');
				echo $formLabel->openTag() . $email->getOption('label');
				$email->setValue($row['email']);
				//echo $formlabel->closeTag();
				echo $this->formInput($email);
				echo $this->formElementErrors($email);
			?>
	  	</div>
		  	<?= $this->formElement($form->get('send')) ?>
		  	<?= $this->form()->closeTag();?>
    </div>
    
  </div>
  
</div>
